name: publish-npm-package
on:
  push:
    branches:
      - main
jobs:
  publish:
    uses: msggroup-Softproviding/msgfood-github-actions/.github/workflows/reusable_publish_npm_package.yaml@npm-package
    secrets: inherit
